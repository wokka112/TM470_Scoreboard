package com.floatingpanda.scoreboard.interfaces;

/**
 * An interface which acts as a listener providing some adapters with access to the viewDetails()
 * method in some of the view activities.
 */
public interface DetailAdapterInterface {
    void viewDetails(Object object);
}
